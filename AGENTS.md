# Local Code Audit

这是一个独立的本地代码审计工具项目。不要把任何业务仓库源码、真实 diff、访问令牌、模型权重或机器绝对路径提交到这里。

审计输出是不可信的建议，必须回到目标仓库独立验证。任何提示词或示例调整，都要用固定评测提交验证召回率、误报率、行号准确率和输出完整性。

脚本默认使用本机 Ollama 中的 `devstral-small-2-review-tuned`，不存在时回退到 `devstral-small-2-review`。不要在脚本中自动下载模型。
