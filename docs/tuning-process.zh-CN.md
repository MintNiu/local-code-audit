# 本地代码审计模型调优过程

本文记录 `devstral-small-2-review-tuned` 从可运行基线到当前版本的完整调优过程、遇到的问题、修复方式和仍然存在的限制。

## 1. 最终目标

目标不是让模型输出更多，而是让它成为一个可高频使用、可重复验证的代码审计模型：

- 尽量提高 P0/P1 真实问题召回率；
- 每条发现都能定位到文件、行号和代码证据；
- 降低风格建议、无依据边界和重复汇总造成的误报；
- API 错误、空响应和输出截断必须明确失败；
- 适用于多个 Git 仓库，并且真实业务数据不进入公共仓库。

## 2. 初始状态

最初的本地模型是 `devstral-small-2-review`，后来已有一个 `devstral-small-2-review` 的 Ollama 派生模型。检查其 Modelfile 后确认：

- `temperature=0.15`；
- `num_ctx=32768`；
- 没有专门的代码审计系统规则和人工确认示例；
- 全局 `local-review.sh` 主要把 Git diff 和审查要求拼接成一次普通请求。

因此没有直接做 LoRA 微调，而是先优化输入、系统指令、示例、运行边界和评测方式。原始模型没有被覆盖。

## 3. 调优过程与遇到的问题

### 3.1 把稳定规则从普通提示词提升为系统指令

原脚本把审查规则放在 diff 后面，规则和待审查内容处在同一个用户输入中。改为通过 Ollama `/api/generate` 的 `system` 字段发送稳定规则，用户输入只承载仓库状态、规则文件、示例和 diff。

同时增加：

- `--context <file>`，允许显式加入相关接口、调用方、DTO、测试等上下文；
- `--examples <file>` 和 `LOCAL_REVIEW_EXAMPLES_FILE`，支持本机私有 few-shot 示例；
- `temperature`、`seed`、`num_ctx`、`num_predict`、`keep_alive` 的环境变量覆盖。

### 3.2 建立派生模型

创建了 `devstral-small-2-review-tuned:latest`，基础模型仍然是本地已有的 `devstral-small-2-review:latest`。当前 Modelfile 参数为：

```text
num_ctx 16384
temperature 0
seed 42
top_k 40
top_p 0.9
num_predict 4096
```

派生模型增加了证据驱动的代码审计系统规则，但没有重新下载或训练基础权重。

### 3.3 发现并修复脚本可用性问题

第一次端到端测试时，在没有传 `--context` 的情况下，Bash 严格模式因为空数组展开直接退出：

```text
context_files[@]: unbound variable
```

修复为只有在上下文数组非空时才遍历。这个问题说明高可用性必须用真实命令链路验证，而不能只做 `bash -n`。

### 3.4 抑制低价值误报

初始测试中模型正确发现了除零和空值风险，但还报告了：

- `final` 类可能影响未来扩展；
- 缺少 Javadoc；
- 没有足够证据的泛化可维护性问题。

随后加入规则：除非项目规则或当前差异证明了具体行为、兼容性、安全或维护风险，否则不报告风格、命名、Javadoc、`final` 或泛化建议。

### 3.5 发现无限输出导致的长时间请求

在加入 few-shot 示例后，一次小 diff 审查曾经超过 5 分钟没有返回。原因不是请求没有发出，而是没有设置最大生成长度，模型持续生成过多解释。

修复方式：

- 设置 `num_predict=4096`；
- 检查 Ollama 返回的 `done_reason`；
- 如果返回 `length`，命令以非零状态失败，并明确提示“模型输出被截断”；
- 不把半截审计结果伪装成成功结果；
- 关闭长时间自动重试，避免单次故障拖延几十分钟。

### 3.6 修正 Java 整数除法语义

模型一度错误地把普通 Java 整数除法报告为“可能溢出”，并声称 `Integer.MIN_VALUE / -1` 会抛出 `ArithmeticException`。

加入 Java 语义约束和负例后，当前规则明确：

- 关注包装类型为 `null`；
- 关注除数为 `0`；
- `int` 的 `Integer.MIN_VALUE / -1` 不应被描述为会抛出 `ArithmeticException`；
- 不把同一根因的具体问题再次汇总成重复问题。

在固定临时路径的一轮合成测试中，模型曾只保留两个有证据的问题：`Integer` 空值和除零。但随后把同一夹具复制到不同临时仓库并重复调用时，模型又生成了整数除法截断、`final`、Javadoc 和重复汇总等误报，因此这次结果不能作为稳定性验收。

### 3.7 发现采样和服务稳定性问题

重复测试发现固定 `seed` 并不能保证这个本地模型在不同请求中完全一致：

- `top_k=1` 会让完整审计请求长时间无响应；改回 `top_k=40`、`top_p=0.9` 后，短审计请求可在约 20 秒返回；
- 同一差异有时返回 2 条 P1，有时返回 5 条（额外包含 P2/P3 误报）；
- 只看退出码不足以验收，因此 `evals/run-synthetic.sh` 现在严格检查正例必须恰好 2 条、负例不能有任何 P0～P3，并单独检查截断故障；
- 连续测试可临时使用 `OLLAMA_REVIEW_KEEP_ALIVE=5m`，但这只减少模型反复加载，不能替代稳定性验证。

后续一次 5 轮门禁在保守 `local-review` 的冷启动路径上出现 clean 对照长尾：第二轮耗时 155 秒，第三轮在 180 秒超时。个人目标实际使用的是 `local-review-local`，因此合成门禁现默认走个人 wrapper（仍可显式覆盖脚本），让稳定性门禁与日常入口一致；切换后同一轮 clean 对照通常为 0–2 秒，URL 查询参数正例约 15 秒，未再出现超时。该调整没有放宽失败条件，只修正了被测运行配置。

后续针对真实的“预签名票据完成/取消后仍可重放”问题做了脱敏留出测试：通用命令的 `top_k=40/top_p=0.9` 连续 5 次只命中 3 次，而 `top_k=1/top_p=1` 连续 5 次全部命中同一条 P1。但在真实配置小 diff 上，贪心采样曾超过两分钟仍未返回。综合召回、响应时间和高可用性，个人 wrapper 与通用命令都保留 `top_k=40/top_p=0.9` 默认值；贪心参数仅作为高影响安全差异的可选覆盖。该选择不增加模型或服务组件，只改变解码策略。

同时修复了大提交的分片边界：以前单个 unified-diff hunk 超过预算会直接失败；现在仅在确实超过预算时按原始行顺序拆成多个窗口，并重复文件/hunk 头保留路径和行坐标。无法拆分的超长单行仍会明确失败，不会静默丢弃差异内容。

真实全量审查还发现分片模型会把未展示的方法误认为“代码被截断”或缺少校验。系统指令现明确声明分片外内容未知，禁止据此生成问题；只有当前窗口可见证据或确定性预检才能成立。这样不会为了减少误报而合并、隐藏真实 finding。

分片回归还使用了 180 行无风险 Java 大 hunk（预算 2500 字节）：修复后完整拆分并返回 clean，未触发“无法拆分”或“代码不完整”误报。

性能审计发现两处纯启动开销：模型探测原先每次轮询固定睡眠 1 秒，且自动选中模型后又重复探测；现改为 100ms 轮询并复用成功探测结果。Java 构建预检也改为一次建立源码索引后复用，避免每个 import 重扫全仓库；这些改动不改变超时、证据范围或分片串行策略。

在最新规则下又重跑了两个真实 `platform-api` 留出提交：`91bff253`（HR 客户端凭据校验，62 秒）和 `2f6c3934`（数据库模式变更，118 秒）均完成且只返回 clean；结果保存在本机私有评测目录，不进入公开仓库。

因此原始长规则和高温度配置未达到阶段 0 回归门槛；后续在 3.8 中通过精简规则和低温度修复了这组公开合成样例。

### 3.8 通过合成回归门槛

将系统指令压缩为短规则、加入 `int a + b` 的明确负例、移除重复注入的私有示例，并把默认温度改为 `0` 后，重新运行严格门禁：

- 除法、URL 凭证泄漏和租户隔离正例共 4 个已知根因，5/5 次均全部命中，输出哈希一致；
- clean 负例（整数加法、内部 token header、租户约束完整查询及维护任务）5/5 次均只输出“未发现阻塞问题”，输出哈希一致；
- `num_predict=1` 的截断故障路径以非零状态退出并提示截断；
- 默认上下文从 32k 调整为 16k，避免当前机器在完整审计请求上长时间无响应。

阶段 0 现在通过，但这只是合成回归门槛；真实历史提交评测仍未完成。

历史提交运行器已用一个本地 `platform-api` 提交试跑。模型这次返回了若干文件和行号，但缺少要求的 P0～P3/信息严重级别，并混入测试缺口建议；新的结构门禁正确以非零状态拒绝，并保留原始输出供人工复核，这类结果不能计入召回率。

### 3.9 为大差异增加确定性分片

真实提交试跑还暴露了一个与 RAG 无关的工程问题：一个只有约 6 KB、但包含多个文件的提交仍可能让单次生成达到长度上限。于是 `local-review` 增加了按 `diff --git` 文件边界、再按 unified diff hunk 边界分片的回退链路：差异超过 3000 字节时，每个分片单独请求模型，结果按分片顺序拼接，不做去重或摘要；任何一个分片超时、截断或缺少严重级别/位置，整次审查都失败，并把已经完成的分片仅以诊断形式展示。无法进一步拆分的单个超大 hunk 会明确失败。

分片不是 RAG：没有向量库、Embedding 或相似度召回，项目规则和显式上下文仍由提示词传入。分片请求会加载本机私有 few-shot 示例，以抑制已经确认的接口契约误报；同时只展示当前分片的文件列表，避免模型根据全局 Git 状态跨分片编造问题。可以通过 `OLLAMA_REVIEW_CHUNK_TIMEOUT_SECONDS`、`OLLAMA_REVIEW_CHUNK_NUM_PREDICT` 和 `OLLAMA_REVIEW_MAX_DIFF_BYTES` 调整。

当前分片预算按 UTF-8 字节计算并固定 `LC_ALL=C`；先按文件，再按 hunk 打包。单个 hunk 仍大于预算时会显式失败，不会把超预算内容偷偷发送给模型。`diff --cc`/combined diff 也会明确拒绝，因为它需要先恢复合并冲突语义，不能按普通单文件 diff 猜测。

这里的字节预算只计算 Git diff；`AGENTS.md`、显式 `--context`、README 和 system prompt 仍会额外占用上下文。因此该门禁不能替代 `num_ctx` 容量检查，项目上下文较大时要提高 `OLLAMA_REVIEW_NUM_CTX` 或拆分输入。

在验证过程中，`platform-api` 的测试文件分片曾在 Ollama 中长时间无响应；设置分片独立超时后，命令会在 60/180 秒边界明确失败，而不是无限等待。一次模型被 Ctrl-C 中断后还曾长期显示 `Stopping...`，停止该 Ollama 模型后合成回归恢复。这些服务层故障不会被计入模型召回率。

在当前规则和路径/格式门禁下，`91bff253`、`2f6c3934` 的首轮输出分别包含 5、12 条泛化误报；加入证据自检和私有接口/租户负例后，`cbe47ea0`、`501ad5a1`、`ccea445b`、`a1284658`、`f1a093a3` 均完整返回 clean，耗时约 10～54 秒。`39955c8` 默认预算会显式拒绝超大 hunk，提高预算后耗时 342 秒且产生 29 条误报。这说明提示隔离和负例能改善已知误报簇，但阶段 1 仍必须逐条人工标注，不能把 clean 结果直接当作召回率证明。

针对 `420ae70c` 暴露的漏报，命令增加了确定性 Java import 预检：只提取当前差异新增/修改的 import，并按同仓库源码索引检查类型是否存在；预检证据按分片隔离后再交给模型。该机制已在该提交快照上识别出 5 个缺失 DTO 的 P1 构建阻断，但外部依赖、生成源码和跨模块类型仍需结合构建上下文人工确认，不能把路径启发式直接当作完整编译器。

独立验证也在 `420ae70c` 的临时 worktree 执行了 `mvn -DskipTests compile`：Maven 编译失败，并列出五个 `Directory*DTO` 缺失符号，与预检结果一致。

后续合成回归发现本地模型偶尔会在有效问题清单末尾追加“未发现阻塞问题”。运行器现在只删除这一条独立矛盾标记，不改写或隐藏任何问题段；同时模型规则明确要求最终输出在 clean 标记和问题清单之间二选一。修复后 5/5 正例、5/5 clean 负例和截断故障门禁均通过，五次重复输出哈希保持稳定。

尝试加入“凭证从 URL 查询参数读取并转发”的安全正例时，模型最初在 180 秒上限内未返回。增加针对性正/负 few-shot，并明确 JDK 标准库依赖不应被猜测为构建问题后，该样本已稳定返回一个凭证泄漏问题；header 传递对照样本返回 clean，现已纳入默认合成门禁。

随后加入租户隔离正/负样本：查询只使用资源 ID 时稳定命中跨租户读取风险；查询同时使用 `tenantId` 和资源 ID 时，经过参数 null/信息级误报边界规则收紧后稳定返回 clean。默认 5 次回归现在覆盖 4 个核心问题根因、4 个 clean 负例和截断故障路径，输出哈希在每个样例内一致。

个人高性能 profile 的 `keep_alive=5m` 不是盲目增加参数：在同一台 Mac、同一个 clean fixture 上，连续两次耗时约为 23s/4s；每次设置 `keep_alive=0` 则约为 20s/21s。它只改善连续审查的第二次加载时间；用户更在意电量时可以覆盖为 `0`。个人 wrapper 和通用 `local-review` 默认都保持 `top_k=40/top_p=0.9`；对已确认的高影响安全差异，可单次覆盖为 `top_k=1/top_p=1`，但应接受更长响应时间风险。

最新个人 profile 的 precision 回归也重新验证了旧误报簇：`91bff253`、`2f6c3934` 和 `39955c85` 在当前私有示例与证据规则下均返回 clean；同时 `420ae70c` 的缺失类型预检和带 `platform-file` context 的 `f6fc2f89` 仍命中真实 P1 根因（同一删除类型根因可能展开为多个受影响引用）。随后用同步后的 tuned 模型复测 `cbe47ea0`（过滤分片不完整误报后 227 秒 clean）、`501ad5a1`（30 秒 clean）和 `ccea445b`（272 秒 clean）。旧的 20 提交 scorecard 没有被直接改写，避免把不同参数阶段的结果混在一起。

针对 `a1284658` 暴露的误报，个人规则补充了 Java `instanceof` 模式匹配的 null 语义、Spring 拦截器异常透传契约，以及 POM/Gradle optional/provided 和 `@ConditionalOnClass` 的外部依赖边界；同时按问题首行合并模型在代码块周围插入的续行，仅用于格式校验，不改写最终输出。复测结果：`a1284658` 返回 clean，`420ae70c` 仍命中 5 个缺失 DTO 的 P1，带显式 context 的 `f6fc2f89` 仍命中跨仓库删除类型 P1；合成回归一轮全部通过。

`2fa4cf1` 首次复测曾因把已有默认值和集中式 `Duration` 边界校验误判为多个问题而输出截断；补充“默认值 + helper 已覆盖 null/非正值/溢出/上限”的边界规则后，同一提交在当前个人 profile 下 35 秒返回 clean。该规则不豁免 helper 未覆盖的具体非法值，也没有提高预测长度。

随后用当前个人 profile 单独复测 `420ae70c33916523afd4f4c6dc21fd6cc5095da0`：耗时 242 秒，返回 5 个缺失 `Directory*DTO` 的 P1 编译阻断，退出码为 0，未出现截断或 clean 标记矛盾。该结果与独立 Maven 编译失败证据一致；耗时偏高来自完整提交快照的分片串行审查，因此没有用提高并发或无限延长超时来掩盖成本。

最新一轮留出回归再次复测历史误报簇：`91bff253`（24 秒）、`2f6c3934`（11 秒）和 `39955c85`（23 秒）均返回 clean；同一轮中的 `de408cc`、`acc6212`、`10b1af3`、`cd11118` 也均返回 clean。此前验证的 `420ae70c` 缺失类型 P1 和带 context 的 `f6fc2f89` 删除类型 P1 仍保持命中。结果只用于精度回归，不代表已有足够样本宣称生产级召回率。

随后复测了 5 个不同功能簇：`501ad5a1`（22 秒）、`ccea445b`（27 秒）、`f1a093a3`（5 秒）、`e4888c70`（8 秒）和 `b4ea10f`（15 秒）均返回 clean。至此当前 profile 已对 14 个真实提交做过精度回归；其中只有经过人工确认的 P1 根因才计入召回率，clean 结果不等价于业务正确性证明。

在完整 5 轮合成回归中，4 个核心问题/安全/租户正例和 4 个 clean 负例均为 5/5 通过，所有重复运行哈希一致；截断故障路径也按预期以非零状态失败。期间发现模型偶尔在问题清单后追加带句号的 clean 标记，运行器现仅移除该独立标记行，不改写或隐藏任何问题段。

为扩大留出覆盖，又复测早期 API/依赖变更 `029e6ff`（21 秒）、`47d7761`（4 秒）和 `1621d8e`（5 秒），以及清单中更早的 `89ea7d8`（34 秒）、`12a1aeb`（26 秒）和 `6dc87ec`（12 秒），均返回 clean。当前个人 profile 已覆盖评测清单的全部 20 个真实提交；其中 18 个为人工确认的 clean 回归样本，2 个为人工确认的 P1 召回样本，仍不足以代表完整生产分布。

跨仓库泛化试跑选取 `platform-file` 的 `7b62671` 临时文件清理和 `2e33eaf` 凭证配置变更；前者在补充后台任务/limit 语义边界后由 4 条误报变为 49 秒 clean，后者 6 秒 clean。随后合成回归一轮再次通过，说明规则没有削弱租户与凭证泄漏正例；跨仓库结果暂不并入 `platform-api` 的 20 提交分数。

维护任务 clean 夹具已加入公开合成回归，覆盖“忽略租户上下文枚举、逐条按租户回收、limit 夹紧”语义。加入后完整 5 轮结果为：正例 20/20、clean 负例 20/20，所有哈希一致，截断故障门禁通过。

跨仓库安全/部署样本 `platform-file:896dca8` 暴露了一个真实漏报：删除版本化已有库迁移脚本并同步删除 README 迁移步骤。加入迁移边界规则后，当前 profile 命中两个受影响位置（同一 P1 根因），并通过一轮合成回归；该根因作为独立跨仓库 gold 样本记录，不并入 `platform-api` 的 20 提交分数。

跨仓库兼容性仍是独立能力边界：在加入“删除类型 + 下游引用”确定性预检后，显式提供 `platform-file` 的控制器和 POM context 可以让 `f6fc2f89` 稳定识别 P1 构建阻断。删除公开契约的风险不能只依赖模型自由推理，跨仓库样本仍需单独计入召回率。
历史评测运行器现在支持重复的 `--context` 参数，并把实际 context 路径记录在私有元数据中；个人 profile 对同一 `f6fc2f89` 跨仓库样本复测耗时 79 秒，返回 4 条受影响类型引用（人工归并为 1 个 P1 根因）。这条结果与单仓库 clean 结果分开计分，避免把不同证据范围混成一个指标。

针对 `platform-file:d984778` 的只读人工审查又确认了三条高价值边界：localhost 配置把 OSS 凭据从环境变量改成字面量（后续 `2e33eaf` 已修复），SINGLE 预签名 PUT 在完成后仍可重放覆盖已登记对象，以及 abort 先删对象再置终态、清理任务只扫 ACTIVE 导致旧票据可重写并留下孤儿对象。个人规则新增了凭据字面量和“完成/取消后票据仍有效”两条证据门槛；不把静态内部 token 的条件性授权风险升级为确定问题。

公开合成回归新增脱敏的 `java-secret-config` 夹具，验证环境变量占位符被提交为字面量 AccessKey/Secret 时能稳定命中；同时保留数据库迁移删除夹具。新增规则后一轮回归结果为：所有既有正/负样例、迁移风险和字面量凭据风险均通过，截断门禁仍按预期失败。夹具只使用带有 `EXAMPLE` 标记的长占位值，不包含真实密钥；长度足够让确定性凭据预检接管该样例，避免把模型措辞漂移误判为规则回归。

2026-09-20 在同步后的 tuned 模型上重新执行 5 轮完整合成门禁，`java-divide`、`java-token-url`、查询参数令牌、租户隔离、迁移、字面量凭据、预签名票据和 4 组 clean 对照均保持每轮哈希一致；最终结果为 `divide=5`、`security_url=5`、`security_query=5`、`tenant=5`、`clean=20`、`migration=5`、`secret=5`、`presigned=5`，截断路径仍按预期失败。

真实 `platform-file:d984778` 留出审查显示，模型可能在问题描述中复述配置里的密钥。运行器现对所有用户可见响应和截断诊断做确定性脱敏，只替换凭据值，不隐藏问题、路径、行号或影响；系统指令也明确禁止模型复制秘密。原始差异仍只发送给本机 Ollama，临时请求文件在退出时清理。

同一留出审查还暴露了 SQL DDL 泛化误报：新增表/字段被错误地逐项要求 `NOT NULL`、枚举、外键和唯一索引。加入 DDL 保守边界后，包含新增初始化脚本和 README 迁移说明的脱敏 SQL 样本返回 clean；删除已有版本化迁移脚本的专门 P1 规则仍保留并单独回归。

又加入脱敏的 `java-presigned-replay` 夹具，固定展示 15 分钟预签名 PUT、取消后旧票据仍可写入同一对象键、以及清理任务只扫描活动会话。个人 wrapper 的贪心采样在该夹具上返回 P1，普通采样曾出现漏报，因此该样例专门用于个人高性能 profile 的召回回归，不把单个样例外推成生产召回率。

为避免直接通过 Ollama/Codex 使用 tuned 模型时规则落后于 wrapper，`config/Modelfile` 已同步加入凭据脱敏、构建完整性、迁移删除、预签名票据竞态、SQL schema 目标和分片边界规则。重建 `devstral-small-2-review-tuned` 仅复用已有基础层；随后完整合成门禁 5 轮通过：`divide=5`、`security=5`、`tenant=5`、`clean=20`、`migration=5`、`secret=5`、`presigned=5`，截断故障仍显式失败且各样例哈希稳定。

真实 `cbe47ea0` 留出审查曾输出“文件内容不完整、请提供完整文件”的 P0 泛化问题，违反分片边界。运行器新增窄范围确定性过滤，只移除这类以输入不完整本身为理由的段落；若段落含具体代码证据仍保留。修复后同一提交耗时 227 秒并返回 clean，专门预检和完整合成门禁均通过，未改变真实 P1 召回或 clean 负例结果。

真实 `a1284658` 曾在第二个分片中重复报告其他分片未展示的类型/方法缺失，并因输出过长而失败。为避免增加源码上下文或默认调用次数，现向每个分片附带本次提交的完整变更路径清单，并明确该清单只是范围元数据、不能作为“当前分片缺失”的证据。修复后同一提交 244 秒完整返回 clean；随后一轮合成回归 `divide=1`、`security=1`、`tenant=1`、`clean=4`、`migration=1`、`secret=1`、`presigned=1`，截断门禁均通过。此前尝试过的截断重试因仍重复误报且额外耗时，已撤回，不作为默认策略。

后续运行时回归补上了三类边界：`--help` 和非法参数在参数解析前不再触达 Ollama；显式 `--model` 只校验指定模型，不重复自动模型探测；中文 clean 标记带句号/感叹号时规范化为标准 clean。输出保护还覆盖 `Authorization: Bearer`、JSON `Authorization: Basic` 等常见形式。构建预检将主源码与测试源码分开索引，避免测试目录中的同名类型掩盖主编译缺失；所有变更均通过预检回归。

模型探测进一步延后到确认存在待审查 diff 之后，干净仓库不会为“没有变更”触达 Ollama。输出过滤先在本地完成分片边界判断和精确去重，再对最终文本做凭据脱敏；一轮合成回归仍保持 `divide=1`、`security=1`、`tenant=1`、`clean=4`、`migration=1`、`secret=1`、`presigned=1` 和截断失败门禁通过。

个人本地运行链路又收紧了只读边界：所有 Git diff、路径和删除文件查询都显式禁用 `textconv`，状态查询禁用仓库配置的 fsmonitor hook，并拒绝以 `-` 开头的 `--base` 值，避免恶意仓库配置或参数把审查变成任意命令执行。用户可见输出会先清理 ANSI/OSC 控制序列，再执行分片边界过滤、去重和凭据脱敏；常见预签名 URL 参数也会替换为 `<REDACTED>`。这些保护只影响本地审查运行时，不改变业务源码或模型权重。

审查结果（单次请求及分片聚合）现在会在最终输出前重新建立全局严重程度顺序（P0、P1、P2、P3、信息），并保持同级问题的原始顺序；这样大 diff 不会因为分片文件名排序而把高优先级问题排在低优先级之后。预检夹具覆盖了跨分片返回 P2/P0 的反向顺序。

### 3.10 加固运行时可信度

在稳定性回归通过后，又补上了三道不依赖模型自觉性的护栏：

- Ollama 的瞬时传输失败默认最多重试两次，但每次重试都会重新计算整次审查的剩余总超时；`OLLAMA_REVIEW_RETRY_ATTEMPTS=0` 可关闭重试，避免网络或服务抖动直接丢失一次审查。
- 输出门禁在结构校验后核对报告中的文件/行号。当前文件或显式 context 文件存在时，超过真实文件行数的定位会失败；删除文件不强行按当前内容校验，避免把历史删除问题误判为行号错误。
- 请求发送前按 UTF-8 字节估算 system 规则、上下文和 diff 的输入 token，并为输出与 tokenizer 波动保留空间。估算超出 `num_ctx - num_predict - OLLAMA_REVIEW_INPUT_RESERVE_TOKENS` 时直接失败，不把可能被静默截断的请求交给模型；默认 16k/4096 profile 已用真实合成回归验证。

`scripts/verify-runtime.sh` 现在还会从 `config/Modelfile` 与 `ollama show --modelfile` 提取完整 SYSTEM 块并比较 SHA-256。规则缺失、格式异常或运行态漂移都会 fail-closed；`evals/test-runtime-verify.sh` 覆盖一致通过和人为漂移失败两条路径。该机制解决的是“脚本已更新但 Ollama 派生模型仍是旧规则”的运维问题，不代表基础模型能力发生变化。

历史评测现在还会在私有 `.meta.tsv` 中记录 `chunk_count`、每个分片的文件路径、diff/提示词字节数、估算输入 token、完成状态和耗时；普通 `local-review` 不创建这些旁路文件。这样大提交的总耗时可以拆解为分片数量、固定提示词大小、单片慢请求和失败重试，调优时不必猜测瓶颈，也不会把诊断信息混进用户审查结果。

本轮新增的 `evals/test-preflight.sh` 回归覆盖了重试次数、超预算拒绝和越界行号拒绝；随后使用当前 tuned 模型跑了一轮完整合成门禁，除截断故障按预期非零退出外，`divide=1`、`security=1`、`tenant=1`、`clean=4`、`migration=1`、`secret=1`、`presigned=1` 全部通过。全局 `~/.local/bin/local-review` 已重新同步，Ollama 模型未被脚本自动下载或常驻保持。

真实 `platform-file:273887d` 评测又暴露了一个门禁兼容性问题：模型把“影响：”“修复建议：”“验证方式：”三个字段连续写在同一行，旧校验只接受字段位于换行开头，因而把合法的 4 条凭据问题和 2 条配置问题误判为格式失败。现在校验要求字段在同一问题段内可解析即可，仍保留严重级别、路径和行号门槛；同一提交修复后以 0 退出并完成输出。该回归也加入 `evals/test-preflight.sh`，防止以后再次把模型的紧凑排版误判为失败。

### 3.11 处理高价值模式的重复漏报

`java-divide` 和 `java-token-url` 曾在不同轮次反复漏报。这不是业务代码自动恢复，而是本地生成模型的概率性：即使 `temperature=0` 和固定 seed，模型重建后的 SYSTEM 规则、输入上下文大小、分片边界、响应预算以及 Ollama 的运行状态变化，仍可能改变输出。此前这两类问题只依赖模型自由生成，合成测试能发现回归，却不能在每次请求中阻止漏报。

处理方式不是继续堆提示词或盲目微调，而是把证据足够明确的窄模式移到运行器确定性预检：

- 新增代码在同一可见 diff hunk 中使用 `Integer` 参数直接除法且没有可见非空/非零保护时，分别输出拆箱空指针和除零两条 P1；普通 `int` 运算、可见保护和业务契约不明的情况不触发。
- 新增代码从 `request.getParameter("x-token")` 等 URL 查询参数读取认证令牌时，输出凭据可能进入访问日志、代理历史或 Referer 的 P1；仅通过内部请求头传递 token 的负例保持 clean。

随后补上了一个容易被分片掩盖的边界：如果除法改动所在的 hunk 没有包含方法签名，预检会只在当前仓库对应文件中向上查找有限范围内的 `Integer` 参数声明，再判断实际除法操作数；常量除法、无关的 `Integer` 参数和注释仍不触发。保护条件还要求出现在除法之前，避免“除法之后才检查”被误当成安全。该场景已加入预检回归样本。

URL 预检也补充了 `authToken`、`accessToken`、`refreshToken`、`sessionKey`、`signature` 和 `credential` 等常见别名，并覆盖同一行 URL 字面量拼接到查询参数或路径的场景；普通 ID、固定 URL 和仅出现在注释中的示例不触发。模型输出的降噪过滤改为按当前 finding 的文件路径隔离证据，避免 A 文件的安全写法把 B 文件的真实问题误删。

随后又覆盖了“先复制再拼接”的常见变体：明确的 `queryValue = token` 等直接赋值会被记录，即使 URL 拼接发生在同一文件的后续 diff hunk 中仍报告 P1；别名按当前源码的方法范围绑定，避免不同方法复用同名局部变量时串线；只对来自已知凭据变量的直接别名生效，普通 ID 别名不会被扩大为安全问题。

方法范围解析随后补齐了真实 Java 排版边界：方法签名可以跨多行，左花括号也可以单独换行；扫描源码大括号深度前会去掉字符串/字符字面量、Java text block 以及行/块注释，避免示例 URL、模板文本、JSON/SQL 中的 `{}` 把后续方法错误归入前一个作用域。大写类常量别名（例如 `QUERY_NAME = TOKEN_HEADER`）允许流入方法范围，token/secret 的已知直接别名可以连续传播；跨行赋值、行内注释和跨行 `getParameter(...)` 也由小状态机补齐，既保持召回，又不重新放开普通小写局部变量的跨方法串线。该回归已加入 `test-preflight.sh`。

2026-09-18 又发现一个同类的 Java 语法边界：完全限定返回类型（例如 `java.lang.String` 或 `java.lang.Integer`）会让旧方法签名正则无法建立方法范围，进而漏掉包装类型除法，或把局部 token 别名退化为文件级作用域。现在作用域解析允许类型名中的 `.`/`$`，并新增完全限定返回类型的除法正例和跨方法 token 别名负例；这只扩展语法识别，不扩大风险模式本身。

同轮留出又覆盖了注解与方法声明同一行的写法（例如 `@Deprecated(...) Integer divide(...)`）。旧参数提取会误取注解的括号，导致 `Integer` 参数未进入除法预检；现在选择方法声明最后一个顶层参数括号，嵌套注解参数不会改变方法参数识别。该边界已加入预检回归，并保持既有合成输出哈希不变。

进一步测试发现，注解数组（例如 `@SuppressWarnings({"unused", "fixture"})`）中的花括号也会干扰方法体边界；作用域扫描现在要求候选参数列表后紧跟方法体或 `throws` 后的方法体，避免把注解花括号当成方法体。新增同一行注解数组的 token 别名跨方法负例，确认不会再退化为文件级作用域。

2026-09-15 又补上了除法扫描器的词法边界：同一行中如果先出现字符串或字符字面量里的 `/`（例如 URL/路径文本），旧实现可能把它误当成运算符而漏掉后面的真实除法；跨行块注释中的 `/` 也不能作为运算符。现在扫描器会跳过字符串、字符字面量、行注释和块注释，只解析可见的运算符 `/`；新增回归样例并完成 5 轮合成门禁，既有除法正例和 clean 负例的输出哈希均保持稳定。

随后补齐了 Java text block 的除法扫描状态：如果新增行只是 `"""...a / b..."""` 文本内容，不能因为同一方法有 `Integer` 参数就误报 `java-divide`。扫描器现在先消费上下文中的 text block/块注释边界，再只对新增代码中的可见运算符记录 finding；新增负例已加入 `test-preflight.sh`。

跨多个 unified-diff hunk 时，text block 或块注释的开始分隔符可能不在当前 hunk 的上下文中；仅靠逐行状态仍会漏掉这类边界。现在除法预检从当前源码快照记录每个行号之前的 text block/块注释状态，在每个 hunk 起点恢复状态，再继续只扫描新增行。新增 `SplitTextBlockDivide` 和 `SplitBlockCommentDivide` 回归确认远离分隔符的文本修改不会被误报。

同样的“别名在上下文之外”也会影响 `java-token-url`：如果别名赋值未改动，而新增的 `getParameter(alias)` 或 URL 拼接行距离赋值超过默认上下文，单看 diff 会漏报。现在安全预检在 Java 文件上从当前源码快照恢复直接 token/secret 别名，再按方法作用域匹配新增使用点；`PreExistingQueryTokenAlias`、`PreExistingUrlSecretAlias` 回归确认能召回，同时不跨方法复用同名局部变量。

令牌字面量的别名解析也统一为大小写不敏感，`"x-token"`、`"X-Token"` 以及带行内注释的多行赋值走同一规则；直接调用和别名调用不会因 header 大小写不同而出现两套召回行为。

随后又修复了除法签名恢复的两个边界：不能从前一个方法借用 `Integer` 参数来解释当前基本类型 `int` 方法；三元、成员访问和调用形式的复杂分母不能简单截断为第一个标识符。现在只有当前方法范围内的参数会进入确定性预检，复杂分母交给模型结合完整上下文判断，相关负例和原有链式/调用表达式正例均加入回归。

需要把“同一问题反复出现”和“同一次审查重复报告”分开判断。前者通常是业务代码在不同提交或不同客户端中再次引入同一模式；例如 `platform-api` 的文件客户端和字典客户端都曾保留从 `request.getParameter(TOKEN_HEADER)` 回退读取令牌的实现，对应不同提交中的新增代码，因此每个提交都可能产生独立的 `java-token-url` 候选。后者才是模型分片或总结阶段的去重问题；同一除法表达式还可能同时有包装类型空值拆箱和分母为零两个独立根因，不能简单合并成一条。当前策略是：确定性预检只针对当前新增证据，模型结果按文件、行号和根因做去重，但不因为“历史上见过”而静默删除问题；如果问题在父提交中已经存在，应标记为 pre-existing 并由人工决定是否纳入本次变更的缺陷统计。

因此，直接运行 `codex` 或 `ollama run` 时仍可能看到概率性漏报、重复摘要或把既有问题带入结果；日常高可用入口必须使用带确定性预检和运行态校验的 `local-review`/`local-review-local`。若业务契约允许，最终修复 `java-token-url` 的根因仍应删除 URL 查询参数回退并只接受受保护请求头；`java-divide` 则应在除法前完成 null/zero 校验或改用经过边界校验的基本类型。审计器负责稳定展示所有可证明问题，不替代业务修复。

针对 `platform-file` 历史中反复出现的 OSS 凭据回退，又增加了配置预检：新增 YAML/Properties/INI/ENV/JSON/TOML 行中的 AccessKey、Secret、API key、密码和 Token 字面量会在本地先报告 P1；同文件可见的非本机 endpoint 会让普通密码/Token 键也纳入检查。空环境变量、示例占位符和 `#` 注释不触发，finding 只保留文件与行号，绝不回显凭据值。该规则仍是窄模式，不能替代密钥扫描器或运行时配置审计。

2026-09-16 对 `platform-file:a9a1d4a` 的独立留出评测先发现模型将“删除数据库记录但同时移除对象存储清理”的真实 P1 漏报为 clean。该模式随后加入确定性预检：同一变更 hunk 中若保留 `fileRepository.delete(...)`、却删除 `fileStorageService.delete(...)`，运行器会在模型请求前报告对象生命周期/孤儿对象风险。评测标签链路同时支持 `missed` 状态，用于记录人工确认但模型没有输出的 P0/P1 根因；修复后重跑将单删和批删聚合为一个 `99-108` 行根因，私有 scorecard 恢复为 1/1 命中、0 误报。原始 0/1 漏报结果仍保留在私有目录，作为规则修复前的对照证据。

2026-09-18 对补充提交 `89ea7d8` 做跨仓库复核时，又确认了一个不能只靠模型提示解决的边界：API 仓库的 workflow claim/ack 客户端没有 `X-Tenant-Id`，而显式的 workflow 服务上下文展示了 `wf_event_outbox.tenantId`、`ignoreTenant` 查询/更新以及只按 `applicationCode`/`eventId` 定位的路径。个人 tuned 模型在三个上下文文件全部传入时仍返回 clean，因此不能把“上下文已提供”当作召回保证。

现在新增了一个严格 opt-in 的跨上下文确定性预检：只有显式 `--context` 中同时出现事件数据的 `tenantId`、`ignoreTenant`/`supplyWithIgnoreTenant` 和事件 claim/ack 证据，并且当前新增或修改的 Java 内部客户端方法携带 `X-Gateway-Token` 却缺少 `X-Tenant-Id` 时，才报告对应方法的 P1；endpoint 注解和令牌头即使只是 diff 上下文，也只有该方法本身出现新增行才会触发。没有服务端绕过租户证据，或客户端已经携带 `X-Tenant-Id` 时保持 clean。回归同时覆盖新增方法、上下文恢复的方法、已带租户头的安全客户端，以及无 `ignoreTenant` 证据的负例。该规则不把普通声明式客户端签名或单独的 `tenantId` 参数升级成漏洞，仍要求人工确认服务端契约和数据库条件。

随后补充了可预测默认凭据边界：新增配置中的 `${...:nacos}`、`${...:admin}`、`${...:password}`、`${...:root}`、`${...:123456}` 或 `${...:changeme}` 不再因为值较短而绕过预检；凭据键中长度至少 16 的非空默认值也会被识别，即使 endpoint 是 localhost。空默认值、短本地占位符和示例占位符仍保持 clean。该规则只作用于当前 diff 新增的凭据键，避免把未改动的历史配置伪装成当前提交问题。

对真实 `platform-file:7b62671` 的两次复测还验证了分片聚合修复：三份配置中的 6 个凭据行均被完整保留，结果文件 SHA-256 两次相同，且没有只有首行的残缺段落。该证据只证明这一个历史样本的确定性路径，不代表所有配置格式或模型发现都已达到 100% 召回。

随后把两类高价值边界继续下沉到确定性预检：不可信 URL 参数直接进入常见 HTTP 出站调用时报告 SSRF，不可信文件名/对象 key 直接进入 `resolve`/`new File` 后再读写时报告路径遍历；同一 diff hunk 中出现明确 host allowlist、scheme 校验或 `normalize` + 根目录 `startsWith` 边界时保持 clean。规则只处理可见的窄证据链，不把所有 HTTP 或文件 API 泛化成漏洞。

- 预检结果按完整问题段去重，不能再用逐行 `sort -u` 破坏“影响/修复建议/验证方式”的归属；模型重复同一位置和同一风险族时只保留一条，但不同根因继续全部保留。
- 对模型用不同措辞重复描述同一凭据暴露的情况，运行器按相同文件、行号范围和凭据风险族保留严重度最高的一条；不同配置行或不同风险族不会被合并，避免“去重”变成漏报。
- 分片聚合也按完整问题段把预检证据路由到每个包含该文件的分片；同一大文件跨多个分片时，每个模型请求都能看到对应证据。最终聚合会按文件/行号和高置信风险族做语义去重，优先保留严重度最高的条目，不再只去掉完全相同的预检段；没有足够证据归类的模型段保持可见。若一个模型段同时包含 URL/除法重复和租户、SSRF、并发等独立根因，则保留整段，不因局部重叠而隐藏其他问题。

语义去重随后在真实大提交 `platform-file:d984778` 上复测：423 秒完成，最终保留 5 条互相独立的 P1 配置问题；此前同一 `platform-file-localhost.yml:40-52` 范围内的多条不同措辞重复报告收敛为 1 条，而 `:65`、`:66` 和 dev 配置中的不同新增行仍分别保留。这个结果验证了去重只合并同一文件、同一行范围、同一凭据风险族的同根因，不会用“去重”掩盖其他问题。

随后新增的边界回归发现，若只按“同位置且包含 token/凭据”判断，理论上可能把配置硬编码与 URL 泄漏两个独立风险错误合并。现在去重同时要求风险族一致，并对配置路径上的泛化凭据描述归入配置族；新增测试确认同一文件/行的两类风险均保留，而同根因的不同措辞仍只保留一条。修改后完整 5 轮合成门禁再次通过。

风险族修复后的真实 `platform-api` 20 提交回归全部完成且无超时/截断；除已知的 `420ae70c` 构建阻断外，`63d520b` 和 `a1284658` 各新增 1 条从 URL 查询参数读取 `x-token` 的 P1 候选。差异中确实存在 header 缺失时回退到 `getParameter("x-token")` 的路径，因此它们不再被静默视为 clean；是否计入正式召回/误报指标仍需结合业务契约人工确认，旧 scorecard 不自动改写。

为防止这类候选在后续重建中回归，合成门禁新增独立的 `java-token-query` 正例，覆盖 `request.getParameter("x-token")`；确定性预检同时追踪 `TOKEN_HEADER` 先赋给局部变量、再传入 `getParameter` 的常见别名写法，避免简单改名就漏报；同时对模型把整份文件标为 `1-9`、预检标为第 `7` 行的重叠范围做了去重回归。修改后 5 轮均稳定命中，且内部 header 对照仍保持 clean。

随后对真实 `63d520b` 和 `a1284658` 做定向复测：两次均以 0 退出，各保留 1 条完整 P1，分别定位到文件客户端和字典客户端的 `getParameter("x-token")` 回退行，没有残缺或重复 finding。它们仍作为待业务确认候选，不直接改写既有人工 scorecard。

2026-09-18 复核时又发现一个会让 `java-divide` “反复出现”的运行器缺陷：除法预检把 unified diff 的未修改上下文行也当成了本次新增证据。只要后续在同一方法中修改其他代码，父提交里早已存在的 `Integer` 除法就会再次被报告。现在预检只允许新增行产生除法 finding，同时继续使用上下文行识别方法签名、参数和保护条件；新增 `PreExistingContextDivide` 回归样例确认既有问题不会因无关修改重复进入当前审查结果。该修复只改变归因范围，不会隐藏当前提交新增的除法风险。

在 `dd46734` 增加参数别名预检后，个人 profile 对 `platform-api` 的 20 个历史提交再次全部成功，17 个保持 clean，3 个非 clean 与已有证据一致。对 `63d520b` 和 `a1284658` 各追加两次完整复测，三次输出 SHA-256 分别保持为 `5bb7f0d8c8e90a9abedd9c0e0ed0872dc13cdfec590077f2459548c484b3d207` 和 `c5d1b2f4da7040638ce8b88c7cb0e50368c41e5416db0e1a39a40f76737f1719`；这证明当前路径稳定，但候选是否属于正式问题仍需人工按业务契约确认。

这套边界把模型从“必须记住两个固定答案”改成“负责开放式审计，确定性代码负责不可漏的已知高价值模式”。`evals/test-preflight.sh` 同时覆盖正例、负例、模型重复和字段连续性；每次修改 SYSTEM 规则或重建 tuned 模型后，必须先通过它和 `SYNTHETIC_REVIEW_RUNS=1 ./evals/run-synthetic.sh`。

合成回归现在默认比较同一夹具第一次运行与后续运行的完整输出 SHA-256；任何内容漂移都会让门禁失败，而不是只打印哈希供人工查看。仅在排查模型波动时可临时设置 `SYNTHETIC_REQUIRE_STABLE_HASH=0`，该结果不能作为稳定性验收。

历史 scorecard 汇总器也改为严格校验必需 TSV 列、提交号、非负指标、完成标记及指标上下界；缺列、重复提交、非法数字或“命中根因数超过人工真值”都会 fail-closed，同时允许保留额外评测元数据列。公开仓库提供 [scorecard.template.tsv](../evals/scorecard.template.tsv)，并用 `evals/test-scorecard.sh` 做输入回归。

确定性预检属于 `local-review` 运行器，不属于直接执行 `codex`/`ollama run` 时的模型能力；高可靠审查必须从 `local-review` 或 `local-review-local` 入口发起。全局安装现在使用指向仓库脚本的符号链接，避免旧复制文件继续运行旧预检；`scripts/verify-runtime.sh` 同时校验全局入口与当前仓库脚本，发现漂移即失败。历史结果应记录运行器版本，否则不能把不同预检版本的召回率直接比较。

真实 `platform-file:273887d` 的复测还发现，模型有时会在自然语言中复述“字面量令牌 <值>”。运行器现在对这类表达也做确定性脱敏；问题级别、路径、行号、影响和修复建议仍完整保留。复测结果中令牌值已替换为 `<REDACTED>`，不会因为增强脱敏而隐藏凭据泄漏这一条问题本身。

随后一次真实配置提交又暴露了异常编码边界：模型响应中出现非法 UTF-8 字节时，macOS `awk` 可能直接报 multibyte conversion failure，导致本应可审查的结果被误判为失败。现在响应进入证据过滤前会先按 UTF-8 解码并替换坏字节；过滤中文分片边界的步骤仍使用 UTF-8 locale，纯结构性去重/排序步骤才使用 C locale。回归测试同时覆盖合法替换字符和自然语言中复述的长凭据值，避免修复编码问题时重新引入凭据泄漏或中文误过滤。

同一仓库的多文件提交还验证了个人 profile 的分片预算：`6000` 字节输入配 `2048` 输出时，配置说明文本可能诱发模型重复生成并截断；改为 `3000` 字节分片和 `4096` 输出后，同一提交完整结束。个人 wrapper 现采用后一组默认值，通用命令仍保留较轻量的 `2048` 分片输出，可通过环境变量覆盖。

## 4. 当前运行链路

```text
Git 状态和 diff
    ↓
项目规则 + 可选上下文 + 本机人工确认示例
    ↓
local-review 的 system 指令与 Ollama options
    ↓
devstral-small-2-review-tuned
    ↓
输入预算与请求重试边界
    ↓
校验 response、done_reason 和截断状态
    ↓
行号、路径、字段完整性与去重门禁
    ↓
输出完整审计结果或明确失败
```

全局命令位于：

```text
~/.local/bin/local-review
```

源代码位于本项目的 `bin/local-review.sh`。真实 few-shot 数据默认位于：

```text
~/.local/share/local-review/examples.md
```

该文件不在公开仓库中。

## 5. 已完成的验证

- `bash -n` 通过；
- `ollama create` 成功创建 tuned 模型；
- 最小 Ollama 请求返回 `done=true`、`done_reason=stop`；
- 空 `--context` 参数的端到端问题已修复；
- 输出达到上限时会明确失败，不返回半截结果；
- 严格回归脚本能够捕获合成样例中的 P2/P3 误报、矛盾输出和截断故障；精简系统指令和 `temperature=0` 后阶段 0 已通过；
- 多文件临时干净样例已验证分片结果可以合并为单一的“未发现阻塞问题”；真实 `platform-api` 分片超时则按预期整体失败并展示已完成分片诊断；
- 本地项目已使用 SSH 推送到公开仓库 `MintNiu/local-code-audit`；
- 公开仓库使用 Apache License 2.0。

## 6. Codex 接入过程中遇到的问题

调优期间也验证了 Codex 与 Ollama 的两种接入方式，这些问题影响的是接入稳定性，不是基础模型权重：

- 直接使用 Codex OSS/Ollama 路径时，模型列表刷新曾因为客户端期待的响应字段与 Ollama 兼容接口返回的 OpenAI 风格列表结构不一致而报错；这不应通过反复下载模型来解决。
- `devstral-small-2` 不支持 thinking。Codex 默认推理强度会触发重连，改为 `model_reasoning_effort="none"` 后，profile 调用成功返回。
- `codex exec review --commit <sha>` 不接受额外的普通提示词参数。提交审查应按该子命令的参数约定调用，避免把 CLI 用法错误误判成模型故障。

最终保留两条互补链路：Codex profile 用于交互式探索，`local-review` 用于可复现的批量审计、输出完整性检查和跨仓库使用。两条链路共享同一个 Ollama 派生模型，但不把交互式成功误认为审计质量已经经过量化验证。

## 7. 当前限制与下一阶段

### 2026-09-18：把回归门禁前移到 GitHub Actions

此前所有预检、分片、聚合、历史标签和运行态校验都依赖本机手动执行。这样规则虽然已经有回归样例，但解析器改动仍可能在下一次本地审查时才暴露。现在增加 macOS GitHub Actions 工作流，针对每次 push 和 Pull Request 运行同一组不依赖 Ollama 的确定性套件，并设置只读仓库权限和执行超时。

这条 CI 只验证脚本、夹具和运行态校验，不上传 Ollama 输出、业务源码、真实 diff 或私有 few-shot 数据；需要本地模型的 `run-synthetic.sh` 和历史评测仍由开发者在本机执行。这样既能阻止 `java-divide`、`java-token-url` 等规则回归，也不会把公开 CI 的通过误认为模型在生产数据上的召回率证明。

同日又做了四个跨服务探索性留出：`platform-gateway:052b848`、
`platform-publishing-service:6e24286`、`platform-integration:fd0f1c4` 和
`platform-hr-service:51709d1` 均使用个人 profile 完整结束且返回 clean；其中 publishing
提交还人工核对了“版本化产物列表”接口保留历史版本是契约行为，不把它误标成当前执行隔离缺陷。
这些结果只作为下一轮人工标注候选，尚未写入正式 scorecard，也不代表跨服务生产召回率。

随后对大提交 `platform-system:57475a8` 做了同一 profile 的完整复核：约 4,000 行迁移
与权限 SQL 被拆分处理，535 秒内完整结束且没有截断，模型返回 clean；对应模块的
`PlatformHrTenantSqlContractTest` 16/16 通过。该样本证明 3,000 字节分片可以完成这类大提交，
但耗时约 9 分钟，只作为高成本探索性证据，不作为日常快速审查的性能承诺或正式召回率。

对 `platform-file:7b62671` 的两次追踪复跑进一步定位了耗时：32 KB 差异被切成 14 片，
前 3 片分别是 README（29 秒）、dev/localhost 配置（45 秒）以及主配置加 Java 文件（43 秒），
其余分片均为 3–6 秒；两次完整结果 SHA-256 一致。慢点集中在配置/README 内容，不能据此
简单增大统一分片预算；此前 6,000 字节配置在更长等待后失败，当前仍保持 3,000 字节默认值，
后续应针对配置分片单独设计低风险优化并重新验证完整性。

进一步把同一提交的三个配置文件单独隔离后，对 `chunk_num_predict=2048` 和 `4096` 做了
对照：两种预算都完整保留 6 条凭据问题，结果 SHA-256 同为
`448a938b0199d2fbc64c4f24c4353cb338539beff8aea781da0d386ca0f712fb`，耗时分别为 123 秒和
125 秒。说明瓶颈不在输出上限，降低 `num_predict` 没有可测收益；当前不改变个人 profile 的
4096 分片输出预算，后续应从提示词重复和模型请求次数入手，并继续以完整性门禁为先。

同轮做了全局禁用 few-shot 的 A/B 作为负面验证：确定性除法和 URL 令牌正例仍能通过，
但 `java-token-header` clean 对照耗时升到 197 秒且输出哈希变化，随后 `java-tenant-safe`
连续 3 次各等待 180 秒无响应，整次审查按失败处理。由此确认 examples 虽增加约 4–5k
输入 token，却是当前模型稳定性和 clean 负例可用性的一部分；不采用全局去除 examples 的优化。

### 2026-09-19：按实际预检证据动态收紧分片预算

真实 `platform-file:f6ce8f6` 首次复核在模型请求前失败：固定提示词估算为 9,095/11,264
token，但首个分片还要附带配置凭据预检和分片文件列表，实际达到 11,367 token，超过预算
103 token。旧逻辑只预留固定 512 token，无法覆盖这种“规则本身没变、证据量变大”的情况。

现在分片预算会在固定提示词探测后，再计算本次变更的分片路径列表和完整预检证据的实际
增量，并动态提高预留值；`f6ce8f6` 的有效分片预算自动从 3,000 调整为 2,004 字节，
8 个分片全部完成，耗时 309 秒，8 条凭据 P1 均保留。该调整只在证据较多时缩小分片，
不删除差异、不省略预检，也不改变普通小提交的请求路径；私有 `.meta.tsv` 会记录实际
reserve 和 effective budget，方便后续复核。

同一 holdout 还暴露了一个与模型无关的 SQL 契约问题：`sql/platform_file.sql` 新增
`CREATE DATABASE platform_file_db`，但保留 `USE platform_db_file`，空库部署会在错误的
数据库上执行 DDL。现在增加了窄范围 SQL 预检：只有同一 SQL 文件中恰好一个
`CREATE DATABASE/SCHEMA` 和一个 `USE`、名称不一致且至少一条语句是新增行时才报告 P1；
多 schema 脚本、只有 CREATE、名称一致和带行尾注释的安全样例均保持 clean。该规则已经
加入 `test-preflight.sh`，不把多数据库迁移的推测泛化成问题。
这是保守的 diff-visible 预检：跨多行拼接、跨文件变量或复杂迁移编排仍交给模型和人工复核，
不会把无法证明的关系强行升级为问题。
随后用真实 `platform-file:f6ce8f6` 的父提交快照做了固定无模型传输验证：当前脚本在
12 个分片全部完成的情况下稳定输出 `sql/platform_file.sql:6` 的 P1，证明该结论来自
预检而不是依赖模型采样；结果和元数据仍保存在本机私有评测目录，不进入仓库。

同样在真实 `platform-file:cf6a214` 中验证了共享内部 token 的重复位置：同一个默认字面量
新增到 5 个配置位置时，预检会保留 5 条文件/行号证据，不会为了按根因去重而隐藏受影响位置。

### 2026-09-18：收紧 fail-closed 配置误报

对真实 `platform-workflow-service:65de3996400acb054246f45207f26db24c2a2de3` 的首次个人 profile 复核发现，模型把“删除本地默认凭据、改为必须从 `${...}` 环境变量注入”误报成了服务启动/连接失败的 P1。该提交的意图是 fail-closed：没有运行时凭据就拒绝启动，而不是恢复不安全默认值。

运行器现在只在证据同时满足以下窄条件时过滤这类误报：finding 指向配置文件；文字明确说删除了默认值并推测缺少环境变量会导致启动或连接失败；对应新增行确实是未带默认值的环境变量占位符；且文字没有独立的硬编码凭据、URL 令牌、租户、权限、SSRF、注入、迁移、并发或重放证据。任何独立根因仍完整保留，不能用“fail-closed”规则掩盖真实安全问题；逗号分隔的多段行号也会正确回溯到配置文件证据。

该规则先用模型重复误报回归验证，再用同一配置文件叠加独立跨租户问题验证不会误删；随后真实提交复跑结果为 clean。模型输出、业务源码和私有评测 manifest 仍只保存在本机，不提交到公开仓库。

### 2026-09-16：大提交预算与分片定位复核

对真实 `platform-file:6b9a262b` 的首次运行在模型请求前被固定提示词预算拒绝；复核发现，预算探测把完整变更路径同时放进“当前分片文件列表”和“全部变更路径元数据”，并把整仓库预检结果提前计入。现在探测只保留一次完整路径清单，预检证据仍按实际分片路由，且分片预算按 `OLLAMA_REVIEW_CHUNK_NUM_PREDICT` 计算。这样没有删除任何变更路径或问题证据，只减少重复元数据。

修复后的真实复测已进入 12 个分片，并保留了 4 条确定性凭据预检；在显式 1200 秒总时限下，最后一个模型请求因总时限耗尽而失败，结果被明确标为不完整，没有伪装成通过。个人入口默认总时限从 900 秒提高到 2400 秒；显式设置 `OLLAMA_REVIEW_TOTAL_TIMEOUT_SECONDS` 仍可选择 fail-fast。该提交尚未获得完整模型审查结果，不能计入召回率或质量分数。

另外，超大 unified diff hunk 的窗口现在会重新计算每个窗口的 `@@ -old,+new` 行号范围，避免第二个及后续窗口继续复用首窗口坐标。`evals/test-sharding.sh` 用纯替换 120 行的 hunk 验证了四个窗口的范围连续覆盖；它与 `test-preflight.sh`、运行态模型校验一起作为脚本改动后的快速门禁。

随后用 `6b9a262b` 的真实 68 文件差异做无模型拆分回归，又发现重算 `@@` 头可能比原头多几个字节，导致 3000 字节窗口被误判为不可拆分。现在分片器为重算头预留增长空间；该提交在假 Ollama 端到端运行中已能完成分片，模型请求结果仍需单独按总时限评估。

当前个人 profile 已覆盖 20 个真实历史提交，其中 18 个是人工确认的 clean 回归样本，2 个包含已确认的 P1 构建阻断；这证明了运行链路和若干已知根因，但样本分布仍不足以声称达到 90% 的生产 P0/P1 召回率。个人 profile 的标签目录与旧基线结果已分离，正式指标仍需把这些样本逐条复核后写入个人标签，而不能直接把模型输出当作真值。下一阶段应：

1. 从真实项目挑选至少 20 个历史提交；
2. 人工确认每个提交的真实问题、误报和无问题结果；
3. 按提交切分 few-shot 示例和留出评测集；
4. 记录召回率、误报率、行号准确率、输出完整性和耗时；
5. 只有 Prompt、上下文和 few-shot 仍然不足时，才评估 LoRA/QLoRA。

未经人工确认的模型输出不能直接作为训练标签，业务源码和真实 diff 不能提交到这个公开仓库。

### 2026-09-21：配置默认值推测过滤实验（后续撤回）

本节保留当时的实验记录，不代表当前行为；后续独立反例证明该过滤存在漏报，见下一节。

真实 `platform-file:cf6a214` 复核再次说明，业务代码中的同一模式会在不同提交重复出现：本次提交在 5 个配置位置新增了共享内部 token 字面量，确定性凭据预检稳定保留了 5 条对应文件/行号问题；同时模型把普通 `${ENV:default}` 默认值具体化误解为部署失败风险，额外生成了 5 组只有“如果/可能/未说明/请确认”等条件式推测、没有契约或实测证据的 P1。

这类重复不能只靠固定 seed 解决：`java-divide` 的空值拆箱/除零和 `java-token-url` 的查询参数泄漏是可由源码直接证明的窄模式，已经由确定性预检负责；而配置默认值是否违反部署契约通常需要外部证据，模型在不同上下文、分片和运行状态下可能反复改变判断。之前尝试把这条边界写进模型 SYSTEM 提示词，虽然减少了配置推测，但导致迁移正例输出漂移，因此已撤回，不把不稳定的 Prompt 变更作为修复。

当时运行器增加了一个按配置扩展名、配置关键词、条件式措辞及排除词表工作的输出过滤。已有无模型预检回归、两轮合成门禁和真实 `cf6a214` 复核均通过，但这些样本未覆盖一般配置缺陷，不能证明不会隐藏独立问题。过滤没有训练模型，也没有证据支撑将删除后的候选数下降视为精度提升。

同日对未纳入上一轮配置留出的真实 `platform-workflow-service:beab22f3` 做个人 profile 复核：39 秒完整返回 clean。该提交只调整 Warm-Flow 租户关联 SQL 的 `utf8mb4_unicode_ci` 显式排序规则，并新增 SQL 合约测试；模型没有把 README 同步或排序规则变更泛化为问题。该结果仍需人工确认后才可进入正式 scorecard。

同日又复核了 `platform-file:bc14e16`：个人 profile 30 秒完整返回 clean。差异新增 `${COMPUTERNAME:NY-TEST-LOCAL}` 作为 Nacos 集群名回退；在非 Windows 环境下它可能让多台机器共享集群名，但当前仓库没有明确部署契约证明该行为必然错误，因此只记录为待人工确认的 P2 候选，不自动扩展确定性预检或正式 scorecard。

同日复核重复模式时又发现两类会制造“反复出现”的预检边界：URL builder 只看整行是否含 `token`，会把 `.queryParam("token", userId)` 或 `String.format("...?token=%s", userId)` 的普通 ID 误判为凭据；别名变量先绑定令牌、随后重赋普通值时，旧 alias 也可能残留。现在 builder/format/`+=` 规则解析实际值参数，别名在每次直接重赋时先清除旧状态；`TokenBuilderUserId`、`FormatUserId`、`QueryTokenAliasReassigned` 和 `UrlSecretAliasReassigned` 负例已加入 `test-preflight.sh` 并通过。该修复不扩大 `java-token-url` 的业务判定范围，只减少同一审查链路中的误报和后续重复修补。

同一轮还验证了 `java-divide` 的方法边界：同一 hunk 里先修改 `Integer` 参数方法、再修改同名 `int` 参数方法时，旧实现会把前一个方法的参数和保护条件带入后一个方法。现在每个改动除法都从当前源码快照独立恢复其包含方法的参数与 guard；`SameHunkMethodScopeDivide` 回归确认 boxed 方法仍保留两条独立 P1，而 primitive 方法不再被重复报告。

同日补齐定位与跨行表达式边界：问题段第一行必须同时携带已变更路径和有效行号，逗号分隔的多个行号逐段校验，避免正文提到正确文件却用无关路径或越界范围通过门禁；Java 除法预检保留跨行 `a /`、`a` 换行到 `/ b` 的短表达式状态，URL 预检覆盖跨行 `+` 和 `StringBuilder.append`。新增对应正负回归后，无模型预检和完整合成评估均通过。这只扩大了可证明语法的覆盖，不会把无法由差异支持的复杂表达式静默升级为问题；预检或传输失败仍按 fail-closed 处理。

### 2026-09-22：修复重复评测的耗时误判，并用真实正负例复核

两轮真实 `platform-api` 复核确认当前运行链路仍能区分 clean 与真实问题：`91bff253` 使用 3 个分片、62 秒完整返回 clean；已确认的 `63d520b` 保留唯一一条 `x-token` 查询参数 P1，`420ae70c` 的构建预检保留一个聚合后的 P1 根因，同时展示 5 个缺失 DTO 的行号和类型，两个正例均完整结束。结果都使用 `devstral-small-2-review-tuned`，运行态 SYSTEM SHA-256 为 `9aa021f8c50feaac17ab2556bbe4485e18d87a726ff0b848e3a5451e9f6c0274`。

随后对 `63d520b` 做两轮完整重复审查。两轮结果文本、模型/规则/参数签名和分片内容一致，但耗时不同；旧的 `run-history-repeat.sh` 把 `initial_status` 和 `chunk_status` 中的耗时字段也纳入签名，错误地报“配置漂移”。现在只忽略耗时，仍严格比较状态、退出码、分片差异字节数、路径、模型和规则哈希；本地回归加入了刻意制造耗时差异的夹具，真实两轮复核通过。该修复影响评测可信度，不改变审查结果本身。

这也明确了可以直接复用的大厂工程模式：确定性扫描器负责高置信证据，模型负责上下文判断和解释，输出必须经过结构化定位/完整性门禁，结果按稳定的代码位置和风险族聚合，所有运行记录模型、规则、输入和输出哈希，并用人工确认的历史提交做 holdout。可以复用这些边界和开源扫描器的组合方式，但不能直接复制任何厂商的内部规则或准确率结论；当前下一步优先级是扩大真实人工 holdout，并逐步引入稳定 finding 身份，而不是继续无边界增加关键词规则。

### 2026-09-21：修复过滤导致的静默漏报与测试夹具串扰

后续补正例时反复遇到路径校验失败。根因不是输出格式或随机性，而是大型预检测试中途的 `git add .`/commit 已把早期配置夹具提交；末尾再返回这些配置路径时，它们不属于待审查变更。删掉失败断言并不能证明过滤安全。

新增独立 `test-config-findings.sh`，只提交基线，再修改配置并创建新配置文件。假模型给出端口 70000 超出夹具中 1..65535 校验范围的完整报告，影响使用“可能启动失败”的措辞。旧过滤器完全没有使用传入的 diff 证据，仅凭“端口 + 可能”就将其删除并返回 clean；同样还在路径/行号/字段校验前删除了非法报告。修复前 7 个用例有 6 个失败。

现在撤回该措辞过滤，不用更多排除词修补它。合法候选原样进入既有定位/结构校验并展示，是否为误报需独立确认；虚构路径、越界行号、缺字段继续显式失败。独立测试修复后 7/7 通过，已接入 `test-preflight.sh`，因此现有 CI 与合成门禁也会执行。测试使用固定假响应，只证明输出链路不会丢掉这些候选，不作为模型召回率或生产验收证据。已有针对可见代码矛盾的其他过滤规则未在本次重写，不能据此声称它们已全部验证。

### 2026-09-22：按根因聚合同一文件的构建阻断

`420ae70c` 的旧结果把 `HrDirectoryClient.java` 中 5 个无法解析的 DTO import 分别输出为 5 条 P1。它们虽然必须逐一展示行号和类型，但共享同一个可修复根因：当前提交快照无法完成该接口的类型解析；按候选条数统计会把一次编译阻断误算成五个问题，也会让历史评测看起来像重复回归。

现在构建预检先按变更文件聚合缺失 import：只有一个类型时保持原有单条措辞；同一文件有多个缺失类型时输出一条规范化 P1，首行列出逗号分隔的全部行号，正文列出每个类型及其行号。这样 scorecard 按稳定根因计数，同时不隐藏任何受影响位置。新增 `MultipleMissing.java` 无模型回归验证了聚合、完整类型清单和单根因计数；真实 `420ae70c` 复核已确认最终只输出 1 个 P1 根因，保留 `3,4,5,6,7` 五个位置和全部 DTO 类型。另补齐了逗号行号预检分片路由回归，避免证据因分片边界消失。

这采用大规模审计系统常见的“证据全保留、根因稳定聚合”边界：确定性预检负责可证明的编译证据，模型仍负责上下文问题，结构化输出门禁和人工 holdout 继续决定是否计入质量指标。聚合不是删除问题，也不能替代更大规模的人工真值评测。

### 2026-09-23：聚合修复后的五轮稳定性复核

在 `5cb0efe` 推送后，使用同一 tuned 运行态执行 `SYNTHETIC_REVIEW_RUNS=5 ./evals/run-synthetic.sh`：除法、URL 凭据、查询参数令牌、租户隔离、迁移删除、字面量凭据和预签名票据 7 类正例均 5/5 成功，20 个 clean 对照全部通过；每个样例五轮输出 SHA-256 一致，`done_reason=length` 截断路径仍按失败处理。该证据说明逗号行号聚合和分片路由修复没有改变既有高置信规则的召回或 clean 边界，但仍不能替代更大的人工历史 holdout。

随后对真实 `platform-api:420ae70c` 做两轮完整重复审查：两轮均 8 个分片、exit=0，耗时分别 81 秒和 52 秒；最终文本 SHA-256 同为
`0d1da3ce59fd07e72b6c6370a99082b7738e807af919a4695b8c0f83d3ed07fe`，且每轮都只有一个 `HrDirectoryClient.java:3,4,5,6,7` 的聚合 P1。重复评测门禁通过，说明耗时变化不会掩盖结果漂移，聚合后的稳定根因可以安全用于人工 scorecard。

这次复核还补齐了评测层的同一位置语义：scorecard 构建和标签迁移现在会把候选中的逗号行号列表与人工标签的单行/范围做区间交集，而不是把 `:3,4,5` 当成路径的一部分。新增回归覆盖聚合候选、标签迁移和非法标签拒绝，避免审查器已经正确聚合但质量指标仍因定位解析失败而失真。

基于该规则在本机生成的 `platform-api:420ae70c` 聚合标签 scorecard 为 `gold=1`、`found=1`、`candidates=1`、`false_positive=0`；旧的五行标签仍作为历史记录保留，不覆盖原始人工证据。新的聚合标签、结果和 scorecard 均位于 `~/.local/share/local-review/evals`，不进入公开仓库。

### 2026-09-23：第二仓库配置 holdout 的独立人工复核

对真实 `platform-file:cf6a214` 使用当前个人 profile 重新运行，8 个分片全部完成，耗时 204 秒，结果 SHA-256 为
`37535a8313cbb463efef560e7e87c36e60f528cc3e0263bf23fcd89f3a24b186`。当前运行器没有使用此前已撤回的配置措辞过滤，因此最终结果完整保留了 11 条候选：5 条来自 `platform-file-localhost.yml` 的共享内部令牌新增，另有 6 条指向普通默认值具体化、服务名可达性和重试配置的候选。

人工按提交差异、生产 Compose 的 `platform-net` 服务拓扑和原默认值逐条复核后，5 条共享令牌均确认为 P1；其余 6 条目前判为误报，原因是 `platform-system` 在目标拓扑中有对应服务，或字段的有效值与原默认值相同，差异本身不能证明运行时故障。独立私有 scorecard 为 `gold=5`、`found=5`、`candidates=11`、`false_positive=6`，召回为 100%；该分数只说明这一个配置/凭据提交，不扩大到通用业务代码的生产召回率。

随后对同一提交做两轮完整重复审查：两轮均 5 个分片、exit=0，耗时分别为 223 秒和 261 秒；两轮文本和 `result_sha256` 均为
`37535a8313cbb463efef560e7e87c36e60f528cc3e0263bf23fcd89f3a24b186`。重复门禁因此确认耗时变化没有造成候选集合或定位漂移，私有重复结果仍不进入公开仓库。

这次复核也验证了当前边界：模型发现必须全部展示，误报通过人工标签进入 scorecard，不能再用没有证据的关键词过滤静默删除。下一步优先增加与配置凭据无关的并发、权限和数据生命周期 holdout，避免继续在同一根因族上重复优化。

### 2026-09-23：补齐跨平台 Nacos 集群隔离漏报

真实 `platform-file:bc14e16` 的原始个人 profile 复核在 32 秒内返回 clean，但差异新增的
`${NACOS_DISCOVERY_CLUSTER:${COMPUTERNAME:NY-TEST-LOCAL}}` 与“按计算机名隔离”的注释不一致：macOS/Linux 通常没有 `COMPUTERNAME`，多台开发机可能回退到相同 clusterName。该问题属于 P2 兼容性/联调隔离风险，不能仅凭模型 clean 结论忽略。

现在加入窄范围确定性预检：只对新增配置中的 `cluster-name` 与 `COMPUTERNAME` fallback 组合报告 P2，显式 `HOSTNAME` fallback 和普通环境变量不触发。复跑结果稳定为唯一一条 `src/main/resources/application-localhost.yml:16` P2，exit=0，耗时 45 秒，结果 SHA-256 为 `580c568d367cc5b22b1c32bcd99dba509f22afa5540b81aa89fd9fec11840896`。同时修复了预检与模型相同措辞在尾部空白不同情况下的重复输出；回归确认该 finding 只展示一次。

这条样本的 P0/P1 scorecard 分母为 0，因此不虚报召回率；P2 真值单独保存在本机标签中。它说明阶段 1 不能只统计 P0/P1，还要持续保留可定位的兼容性问题和模型漏报证据。

规则变更后的 `SYNTHETIC_REVIEW_RUNS=1 ./evals/run-synthetic.sh` 通过：7 类正例全部命中，4 个 clean 对照通过，截断路径显式失败；随后完整快速回归也通过。该回归只证明既有高置信规则没有退化，不替代跨项目人工 holdout。

另对跨服务 `platform-gateway:052b848` 做人工 clean holdout 复核：新增 workflow 路由应用绑定和目标租户授权测试与现有 Nacos 路由契约一致，未发现可由差异支持的 P0/P1/P2/P3 问题。当前结果完整返回 clean，scorecard 为 `gold=0`、`candidates=0`；它增加了跨服务 clean 覆盖，但不增加 P0/P1 召回分母。

再对 `platform-hr-service:51709d1` 做并发/状态同步方向的人工 clean holdout 复核：外部人员 `active` 状态同步为 `ACTIVE/INACTIVE`，空值仍按历史兼容语义视为 active，新增的非活动人员断言通过，模块测试 `mvn -q -Dtest=DirectoryImportTransactionServiceTest test` 通过。模型结果完整 clean，scorecard 为 `gold=0`、`candidates=0`；该样本增加状态生命周期覆盖，但不增加 P0/P1 召回分母。

再对 `platform-integration:fd0f1c4` 做重试/限流方向的人工 clean holdout 复核：新增 QPS、次数过多和暂时限制关键词与既有 HTTP 429/指数退避链路一致，权限错误仍不可重试，`mvn -q -Dtest=HttpDingTalkApiTest test` 通过。模型结果完整 clean，scorecard 为 `gold=0`、`candidates=0`；该样本增加外部调用可靠性覆盖，但不增加 P0/P1 召回分母。

同日继续复核发布服务更近的提交 `platform-publishing-service:129a244`：该提交新增印刷页标、重复表头配置，并同步质量报告、问题证据和页级复核链路。当前个人 profile 连续两次完整结束（均 exit=0，均返回 clean），未出现截断、空响应或路径定位失败；人工逐文件检查确认新增字段在确定性执行器、持久化同步、查询 VO 和示例契约之间保持一致。`TypesetEvidenceApplicationTest`、`DeterministicTypesetExecutionHandlerTest`、`TypesetProfileApplicationTest` 和 `ProfileExamplesContractTest` 均通过（仅有 Mockito/Byte Buddy 动态 agent 警告）。该样本补充了跨服务、跨时间的 clean 稳定性证据，但没有新增已确认 P0/P1 根因，不计入召回率分母。

文档提交 `2bf483b` 推送后又执行一次完整合成门禁：7 类正例均 exit=0 且各命中 1 次，4 个 clean 对照均无问题，所有响应完整，`num_predict` 截断路径按预期显式失败。该复跑只验证公开仓库、全局入口和本机 tuned 模型仍处于同一规则版本，不改变阶段 1 的真实人工标签分母。

随后补充跨仓库登录路由留出 `platform-gateway:7592b4d`：提交只在三套 Nacos 配置中新增 `/mobile/login` 到认证服务路由和白名单；显式附带 `platform-auth` 的移动登录控制器与 Spring Security 配置后，个人 profile 两次均完整返回 clean。第一次默认 16K 窗口按预算门禁拒绝发送（固定提示词 11362/11264），没有静默截断；仅对该次把 `OLLAMA_REVIEW_NUM_CTX` 提高到 32768 后两次均成功。网关 `AiGatewayRouteContractTest`、`SaTokenAuthGlobalFilterTest`，以及认证服务 `LoginServiceImplTest`、`LoginControllerTest`、`SystemLoginSecurityIntegrationTest` 均通过。该样本补充跨服务认证路由覆盖，不增加已确认 P0/P1 根因；它也验证了跨仓库上下文应按需扩大窗口，而不是降低完整性门禁。

随后复核 `platform-integration:18ea173` 的 HR 目录下游失败传播：变更为批次创建、分片上传和批次完成统一保留下游错误码与消息，并显式附带 `platform-api` 的 `HrDirectoryClient` 契约上下文。个人 profile 两次均完整返回 clean；`HrDirectoryPublisherTest` 通过。该样本补充外部调用错误处理和跨仓库契约覆盖，不增加已确认 P0/P1 根因。

继续复核 `platform-hr-service:c54baaf` 的内部人员树查询：提交新增组织树、当前有效任职挂载、关键词裁剪和账号状态范围；个人 profile 两次均完整返回 clean，分片预算在固定提示词较大时自动收紧到 2616 字节。人工核对确认人员查询最终进入带租户条件的 `HrPersonMapper`，并通过 System-HR 的 ACTIVE employment 范围限制可见账号；`HrPersonnelDirectoryApplicationTest`、`HrControllerContractTest` 通过。该样本补充人员目录的租户、状态和容量边界覆盖，不增加已确认 P0/P1 根因。
